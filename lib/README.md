# Private Libraries

This directory is intended for project-specific (private) libraries. 
Currently, all core modules are built into `src/`, but if you write a modular component that needs to be completely decoupled (like a custom display driver, peripheral framework, or external math library), you can place its source directory here.

PlatformIO will automatically compile it as a static library and link it to the executable.
